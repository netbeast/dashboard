# Packages #
############
# it's better to unpack these files and commit the raw source
# git has its own built in compression methods
*.7z
*.dmg
*.iso
*.jar
*.rar
# *.gz
# *.tar so it uploads test prepackaged app
*.zip

# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db


# Project ignores #
###################
# .env is forcely added
.env

# public/dist/* keep track of dist files so we can avoid installing devDependencies

.log
.sass-cache/
.ftpconfig

node_modules
_apps/*
!_apps/installed_apps_live_here
*.sqlite

node_modules

test/coverage
npm-debug.log
.errorlog

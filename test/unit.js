/* global describe it */

// File to unit tests those functions uncovered by functional tests
// jesus@netbeast.co

// var broker = require('../src/helpers/broker')

describe('unit tests', function () {
  it.skip('should emit notifications', function (done) {
    // broker.emit('my %s interpolated', 'string interpolated')
    done()
  })
})
